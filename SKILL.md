---
name: indigo-riso-poster-skill
description: Generate limited-palette indigo-blue-and-cream poster/wallpaper images using a duotone riso-print halftone-dot technique — NOT photo editing, NOT full-color illustration. Use this skill whenever the user asks for a "蓝白海报"、"靛蓝极简壁纸"、"网点版画风格图"、"限定蓝白色系的意境海报", or describes wanting a calm, negative-space-heavy poster built from a single theme/mood/word (e.g. "深夜的海"、"连绵的雾山"、"一滴墨扩散开") rather than from an uploaded photo. Also trigger when the user mentions riso print, halftone dots, duotone, 半调网点, 丝网印刷感, or wants something visually consistent with a "蓝白系列" of posters/wallpapers. Do NOT use this skill for photo-to-illustration tasks (that's a different workflow), for multi-color branding work, or for realistic/photographic renders.
---

# Indigo Riso Poster / 靛蓝网点版画海报

一个把一句主题词 / 一个意象转化为限定"靛蓝 + 米白"双色系海报的 skill。风格核心是**用网点疏密代替传统渐变**，模拟丝网印刷（riso print）的双色套印质感，而不是数字插画的光滑渲染。

这不是从照片提炼重画的 skill（那是另一类工作流），输入永远是**一句话、一个意象、一个情绪，或一个主题词**，不需要参考图。

## 何时使用

- 用户想要"蓝白海报""靛蓝壁纸""网点质感的图""丝网印刷风格的意境图"
- 用户给了一个抽象意象但没给照片：一片海、一座山、一朵云、一滴墨、一个地平线场景
- 用户明确提到 riso / halftone / duotone / 半调网点 / 版画感
- 用户想要和已有的"蓝白系列"保持视觉一致（同一套色卡、同一套网点逻辑）

## 核心原则（三层配方）

这套风格靠**严格约束**而不是"随便调一个好看的蓝"撑起来。生成前必须确认三件事，缺一不可：

1. **色卡**（用哪一组靛蓝+米白）——见 `references/color-cards.md`
2. **构图类型**（画面怎么分割、留白怎么摆）——见下方"构图规则"
3. **网点渐变怎么用**（渐变和材质分别怎么表现）——见 `references/halftone-spec.md`，**生成前必须读这份文件**，网点规范不是可选的装饰细节，是这套风格能不能被认出来的关键。

## 工作流

### Step 1 — 理解意象

用户给一句话或几个词。提炼出：
- 一个**唯一的视觉锚点**（山 / 海 / 云 / 墨迹 / 一个点 / 一条地平线上的形状——只能有一个主体，不能并列多个）
- 这个意象的情绪基调（安静 / 辽阔 / 孤独 / 清晨 / 深夜……），用来决定选哪张色卡

如果这个意象的主体本身就是模糊的、说不清是什么、或者故意让人产生不止一种联想（不是"一座山"这种明确事物，而是"说不清是什么的一团形状"），先读"主体扩展规则 → 抽象具象暗示型主体"（在 Step 3 之后），按那份规则的写法处理，不要按普通具体事物硬套。

如果用户给的输入本身不是一个画面、而是一个抽象概念（比如"歌曲"、"思念"、"自由"这种没有天然视觉形态的词），先做一次**具象化翻译**，再继续正常流程：

1. 从这个概念联想出 2-3 个候选具体意象（比如"歌曲"可以联想成"一张唱片"、"一段五线谱"、"扩散的声波"）
2. 挑其中最适合"单一锚点+网点消散逻辑"表现的一个——优先选那些天然带有"扩散/消散/延伸"物理过程的意象，因为这套系统的表达力主要靠网点渐变，选一个能让网点渐变"名正言顺"发生的意象，比选一个纯静态的意象效果更好
3. **在最后的"一句话阐释"里保留这次翻译的痕迹**，不要把"歌曲"直接说成"这是一张唱片"就完了，要让用户看得出这是一次主动的翻译决定，方便用户否决或者引导到别的方向（例如"把'歌曲'想象成一张正在被听见的唱片，声音的痕迹一圈圈变轻"）
4. 这个翻译没有标准答案，不同的具体意象选择都可能成立，如果用户后续说"我想的不是这个"，直接换一个候选意象重来，不用坚持第一次的选择

### Step 2 — 确认色卡

读取 `references/color-cards.md`，向用户展示三套色卡的简短描述（深海藏青 / 雾山晨蓝 / 墨夜留白），让用户选一个，或者根据 Step 1 的情绪基调直接推荐一个并说明理由，用户没有异议就直接用。

不确定时的默认规则：
- 白天 / 辽阔 / 清晨的意象 → 深海藏青版
- 有远近层次、山脉云雾类 → 雾山晨蓝版
- 深夜 / 内敛 / 反差强的意象 → 墨夜留白版（反转）

### Step 3 — 确认构图类型

**判断逻辑改成按"主体的行为逻辑"分类，不是按表面看到的是什么物体**——先问自己：这个主体是静止陈列、还是带扩散/消散的动作、还是带深度/密度的走向、还是需要一条分割线？想清楚行为逻辑，再选下面对应的类型，不要看到"竖版"就条件反射选纵向延伸型，看到"小"就条件反射选静态陈列型。

五选一（可以在推荐后直接问用户，或根据意象自动判断）：

- **横向地平线型**：画面被一条水平线分成两部分（海天、山云），上下各自承担不同的留白/实色比例。参照案例：海面+天空、云海。
- **静态陈列型**：主体不动、没有扩散或深度动作，纯粹靠留白面积撑气氛。参照案例：一个小圆点、一把孤立的椅子、一盏窗光。**这类构图最容易出现"整张图零纹理渐变"的问题**——生成后必须专门检查阴影、材质或背景角落里是否安放了至少一处网点或网纹（见 halftone-spec.md 的强制底线），不能因为主体本身"很安静"就整张图没有一处纹理签名。
- **扩散消散型**：核心主体+向外用网点扩散消失的边缘。参照案例：墨迹、裂痕、涟漪、烟。**判断标准是"动作"而不是"体积"**：哪怕核心物体很小（比如声音涟漪的唱片），只要带有向外扩散的网点动作，就归这类，不归静态陈列型——一旦出现同心圆/放射状网点，就是扩散逻辑，需要扩散消散型更大的面积上限才撑得起层次感。
- **密度延伸型**（新增）：主体沿某个方向从清晰走向浓重/深邃，用网纹（不是网点）表现深度和压强。参照案例：潜入深水的生物、渐渐被阴影吞没的物体。这类的"留白"不必是纯 Ground 空白，可以是一片有网纹纹理的深色场——深度层次本身需要空间才能读出来，面积上限比照扩散消散型。**和纵向延伸型的区别**：纵向延伸型的"另一端"是消失于纯留白（比如楼梯顶部的天光、线香的烟最终归于纯背景色）；密度延伸型的"另一端"是走向更深更重的有纹理色场（不是变淡消失，是变浓变深）——方向判断反了会导致该用网纹的地方用了网点，逻辑对不上。
- **纵向延伸型**（竖版专用）：主体沿画面纵轴延伸，一端具体清晰、另一端消失于纯留白或雾化于网点中，两端之间形成"低处/高处"或"近处/远处"的张力。参照案例：一段向上消失的楼梯、一缕升起消散的烟。竖版海报统一用 3:5 比例，优先考虑这个类型，横版也可以用但效果不如专属横向逻辑的地平线型。

## 主体扩展规则（Step 1 的补充，非独立步骤）

本节和"次要装饰元素例外"（在反面清单之后）是同一类内容——都是对"只能有一个主体"这条基本规则的补充和例外，只是位置分开放，因为"次要装饰元素例外"更适合紧挨着它要修正的那条反面清单读。

### 抽象具象暗示型主体

目前 SKILL.md 里所有主体词汇（山、海、脚印、裂痕、线香）都是明确可指认的具体事物。这类主体不同：**形态本身故意留有解读空间，可能同时像两种东西，又都不完全是**（比如一团墨迹的轮廓，既像侧脸又像山脊；一个手势般的形状，既像挥手又像波浪）。

使用规则：

1. **这个模糊形态本身就是唯一锚点**，不能因为它"看起来像两样东西"就当成两个主体处理——它仍然只占一个锚点的面积配额，不能突破对应构图类型的上限
2. **写 prompt 时用联想式描述，不要直接点名两个物体叠在一起**：正确写法是"形态让人联想到侧脸轮廓，又像是山脊的延伸，边界故意保持模糊"，错误写法是"一张侧脸和一座山叠在一起"——后者会让模型画出两个清晰物体的拼贴，而不是一个真正暧昧的单一形态
3. **网点消散在这类主体上通常用于强化暧昧感**，而不是单纯表现物理扩散——边缘越模糊，越难一眼确定"这到底是什么"，这是这类主体的核心表达手段，不是缺陷
4. **构图类型通常适用扩散消散型**，因为暧昧形态天然带有"正在成形/正在消散"的观感

**示例 prompt（重构自最早的风格参考图之一，用我们自己的色卡语言）**：

> 生成一张限定"靛蓝 + 米白"双色系的极简海报，采用丝网印刷双色套印质感，横向 16:9 构图。
>
> 色卡：纸基色(Ground) `#F0E7D9`；主墨色(Ink) `#1E3F73` 深靛蓝，构成画面右侧一个饱满的有机形态；雾层色(Mist) `#A9BFDA`，表现形态后方两个更浅的抽象轮廓层次。
>
> 主体意象：画面右侧偏下方，一个不规则的深靛蓝有机形态，边界起伏、没有明确的几何逻辑——形态的上半部分让人联想到低垂的侧脸轮廓，下半部分的曲线又像是抬起的手臂，两种联想都成立，也都不完全成立，不要刻意强化任何一种解读。形态左侧边缘用网点向外扩散消失于米白留白。形态后方叠加两层更浅的雾蓝色抽象轮廓，形似衣物褶皱或羽翼，半透明水彩质感，不需要和前景形态有清晰的物理连接逻辑。
>
> 构图类型：扩散消散型。整个形态（含后方两层雾蓝轮廓）总占比不超过 45%，画面左侧大片纯留白。
>
> 禁止出现：靛蓝/雾蓝/米白之外任何色相；把形态画成两个可清楚分离、边界分明的具体物体（一旦能一眼确认"这是脸"或"这是手"，暧昧感就没了，需要重新调整）；文字。

**一句话阐释**：说不清是谁的轮廓，但那不重要，重要的是它正在散开。

### Step 4 — 生成

用 `references/prompt-template.md` 里的模板拼出完整生成提示词，代入：
- 选定的色卡三色值
- 选定的构图类型对应的具体规则
- 网点渐变的使用位置（严格按 `references/halftone-spec.md`）
- 主体意象的具体描述
- **文字决策（落款 / 呼应 / 节奏，以及四变量取值）**——默认配文，文字段落在生成前按 `references/typography-spec.md` 写好并拼入 prompt，文字与画面一次生成，不做后期叠加；仅当用户明确说"不要文字"时才省略

调用图像生成能力产出海报。默认宽高比 16:9（横向海报/壁纸感），用户要竖版时切 3:5。

### Step 5 — 输出三件套

每次生成后固定输出以下三项，方便用户在此基础上微调重跑，而不是每次都要重新解释一遍需求：
1. 最终海报图
2. 本次实际使用的完整生成 prompt（方便用户微调重跑）
3. 一句极短的创作阐释（不超过一句话，不要写成营销文案）

**兜底规则：如果当前环境无法直接生成图片**（没有图像生成能力、API key 缺失、或工具调用失败），**必须输出完整的生成 prompt 给用户**，让用户可以复制到其他有生图能力的工具里使用。此时输出两件套：完整 prompt + 创作阐释，并明确告知"当前环境无法出图，请将此 prompt 复制到 [具体工具名] 中使用"。

### Step 6 — 迭代

用户常见的调整请求和对应动作：
- "留白再多一点" → 主体锚点面积再压缩（参考 halftone-spec 里的面积上限）
- "层次感不够/太平" → 检查是否用了雾层色和网点渐变表现远近，而不是只有两个纯色块
- "看起来像普通渐变海报，不像网点版画" → 这是最常见的翻车模式，说明模型抄近路用了模糊渐变代替网点，必须重新按 halftone-spec 里"禁止用径向/线性渐变代替网点"的规则重跑
- "换一套色卡但构图不变" → 只替换三色值，锚点形状和构图规则保持不变，确保系列感

## 硬性反面清单

生成前后都要自查，出现以下任一情况视为不合格，需要重新生成：

 - 出现了靛蓝家族 + 米白/雾蓝之外的第三种色相（绿、红、黄、紫等），**且不符合 `references/color-cards.md` 里"专色点缀（Accent）"的硬条件**（必须是"意义奇点"——拿掉它意象的核心意义就塌了；面积在静态陈列型上限内；默认只能一处，符合分布式点缀条件时可多处）——符合条件的 Accent 点（光/花/生命等）属于允许的例外，不算违规
- 用了玻璃质感、3D 渲染反光、赛博朋克霓虹光
- 用传统模糊渐变（gradient blur）代替网点渐变表现层次或阴影
- 出现多个并列的视觉主体，导致画面没有唯一焦点，**且不符合下方"次要装饰元素例外"的条件**
- 线条/形状过于圆滑、卡通贴纸感（这套风格是版画的硬边+网点，不是插画的柔和描边）
- 主体色块占比超出该构图类型规定的面积上限（见 halftone-spec）
- 无意义地塞入大段文字（默认带一句极简配文，除非用户明确说不要；配文时必须遵守 `references/typography-spec.md`：极小、极克制、与画面同一层印刷、四变量齐全）
- 文字出现写错字/拼写错误/笔画糊乱——写错的文字直接重生成，不当"风格"接受

## 次要装饰元素例外（多个元素何时不算"并列主体"）

原本"只能有一个视觉锚点"的规则，现在有一条界限清楚的例外：**多个元素如果明显是次要的、烘托性质的，不与主锚点竞争视觉焦点，可以不算违规**（对应最早参考图里那种画面四角分布多组云团的构图）。

判断标准，必须同时满足：

1. **视觉权重明显更低**：次要元素的网点密度、色彩饱和度或清晰度要低于主锚点，一眼能分辨"这些是背景装饰，那个才是主体"——不能几个元素同样清晰同样突出
2. **功能是材质/氛围，不是叙事**：次要元素属于 halftone-spec.md 里的"场合 B：网点表现材质肌理"，不承担"正在发生的动作/变化"这种叙事功能（叙事功能是主锚点专属的）
3. **排布方式偏装饰性，不是随机散落**：对称、成组、沿画面边缘分布这类有规律的排布，读起来像"画面的框"，而不是"好几个故事同时在发生"；杂乱无章地撒在画面各处，容易被读成真的有多个主体，不满足这条
4. **仍然要有一个更突出的主锚点**：即使允许多个次要元素，画面必须还有一个视觉权重明显更高的东西作为唯一焦点——如果拿掉主锚点、只剩这些次要元素，画面不能成立

**示例**：画面四角分布几团用网点表现的云（次要、材质型、对称排布），中央有一个更突出的月亮或飞鸟剪影（主锚点，网点密度更高更清晰）——这种情况下多组云不算违反"单一锚点"规则。但如果画面里有两只对等清晰的鸟，各自都在"做什么"，那就是两个主体，仍然违规。

## 内容结构

```text
indigo-riso-poster-skill/
├── SKILL.md                        # 本文件：工作流与约束
├── references/
│   ├── color-cards.md              # 三套色卡的精确色值与使用场景
│   ├── halftone-spec.md            # 网点渐变技术规范（必读，核心签名）
│   ├── prompt-template.md          # 中英文生成提示词模板
│   └── typography-spec.md          # 配文排版规范（角色驱动，非固定模板）
└── assets/
    └── examples/                   # 参考样图（不要复用其中的具体主体/构图，仅用于风格校准）
```

## 可自由调整的部分

这套系统是高质量起点，不是不可变规范。可以按项目需求调整：
- 色卡的具体色值（保持"三层角色"结构：纸基色 / 主墨色 / 雾层色）
- 构图类型可以新增，但新增时要同样给出量化的留白/占比规则，不能只写"好看就行"
- 宽高比可按用途调整（封面图、壁纸、社交媒体头图等），但网点规范和色彩纪律不应放松
